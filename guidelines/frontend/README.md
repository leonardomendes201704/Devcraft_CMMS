# Frontend Guidelines

- Use React Router for navigation and TanStack Query for remote state.
- Keep forms with React Hook Form + Zod.
- Keep all user-facing text translatable via i18next.
- Prefer shared UI primitives before page-specific components.
